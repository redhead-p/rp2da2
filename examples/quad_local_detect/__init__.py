""" Package

This contains the example main module for a quad local detector.

Note this configuration is only currently available on the Arduino Nano RP2040 Connect
as this makes four ADC pins available. An RP2350b chip has more ADC channels so this could
potentially be used but there are no Pico variants based on this chip as yet.


Common modules are in the lib package and modules associated with RailCom Channel 1 are located in the rp2dcc
package.

There is no PCB design associated with this configuration. It would need to breadboarded.
"""
