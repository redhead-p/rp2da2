# example main modules

The examples directory provides example main.py modules for command station and local block detection.

These modules contain installation specific code and may need to be edited.
